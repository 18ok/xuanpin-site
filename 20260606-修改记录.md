# 选品心决网站 · 2026-06-06 修改记录

> 日期：2026-06-06
> 参与：Reasonix（执行）、玉成
> 传给：Cursor
> 项目路径：`C:\Users\an'kang\Desktop\xuanpin-site\`

---

## 一、今日改动清单

### 1. 改过的旧文件

| 文件 | 改动 | 原因 |
|------|------|------|
| `index.html` | `<p class="hero-eyebrow" id="hero-title">` → `<h1 class="hero-eyebrow" id="hero-title">` | 页面缺少 h1，文档大纲不合规范 |
| `index.html` | `#reader` 上去掉 `aria-hidden="true"`，只保留 `hidden` | 双重隐藏冗余，reader.js 会在 open/close 时动态管理 aria-hidden |
| `index.html` | 第71行内联 `style="font-family:…;font-size:…;text-align:left;"` 替换为类名 `reader-inline-link--title` | 内联样式应收进 CSS |
| `index.html` | `<body>` 后添加 `<noscript>` 警告条 | JS 被禁用时给用户提示，不做强制弹窗 |
| `assets/tokens.css` | `--bg: #f6f6f4` → `#faf9f7`、`--ink: #2e2e2e` → `#3a3a3a` | 对齐设计稿 `选品网站构思-20260606.md` 的色号 |
| `assets/base.css` | 新增 `.reader-inline-link--title` 类 | 承接上述内联样式提取 |
| `一键发布到GitHub.bat` | 在 git push 前插入 `python build.py` 编译步骤 | 配合新的 Markdown 编译流程 |

### 2. 新建的文件

| 文件 | 作用 |
|------|------|
| `build.py` | Markdown → HTML 编译脚本。读取 `posts/*.md`，生成 `content/*.html`，自动更新 `data/manifest.json` |
| `posts/interview-01.md` | 采访01的 Markdown 源文件（已验证编译输出与原有 HTML 一致） |
| `posts/.template.md` | 新采访模板，复制改名即可写新文章 |

### 3. 没动的

- reader 浮层 —— 确认保留。它不是打断用户的弹窗，是用户主动点击后打开的沉浸阅读空间，毛玻璃效果是为让文字"浮于眼前"专注阅读。这是选品心决的核心交互，不删。
- `data/manifest.json` —— build.py 自动维护
- `content/interview-01.html` —— build.py 编译输出，与原始版本一致

---

## 二、新的工作流

```
在 Obsidian 写完采访（Markdown）
    → 保存到 posts/interview-XX.md
    → 双击「一键发布到GitHub.bat」
        ├── python build.py （自动编译）
        │   ├── .md → content/*.html
        │   └── 更新 manifest.json
        └── git push → GitHub Pages 自动部署
```

### Markdown 写作规范

- **front matter**（YAML）：`id`、`title`、`quote`、`category`、`years`、`date`、`status`
- **问答**：`**问：** xxx` / `**答：** xxx`（编译后自动转为 `<p class="speaker">问/答</p>`）
- **判断清单**：`- [ ] 规则文字`（编译后转为带 checkbox 的 `<ul>`）
- **文末 CTA**：
  ```
  [cta]
  title: 拿一个你在看的品，试算毛利
  text: 案例说明
  button: 打开毛利粗算
  target: calculator
  [/cta]
  ```

---

## 三、待 Cursor 继续的工作

1. **验证**：在浏览器打开 `index.html`，确认 reader 浮层加载 `interview-01` 正常，毛玻璃效果和 CTA band 按钮正常
2. **采访02/03**：manifest.json 里已有 interview-02（百货·五年）和 interview-03（潮玩·一年）的条目，`content/` 下缺少对应 HTML。可以：
   - 在 `posts/` 下写 `interview-02.md`、`interview-03.md`
   - 跑 `python build.py` 编译
3. **简报 Markdown 化**：目前 `content/brief-01.html`、`brief-02.html` 还是手写 HTML。如需要，`build.py` 可以扩展支持 briefs（但当前只处理 interviews）
4. **GitHub Actions 自动编译**：目前编译靠本地 `build.py`。如果要在 push 后自动编译（不用手动跑），可以在 `.github/workflows/pages.yml` 里加 `pip install markdown && python build.py`

---

## 四、相关参考文档

| 文档 | 路径 |
|------|------|
| 网站设计构思 | `D:\我的文学\AI对接专用\传话\战略与全案\选品网站构思-20260606.md` |
| 交接总纲（合规/技术约束） | `D:\我的文学\AI对接专用\传话\战略与全案\交接助手-商业合规与技术总纲-v0.2-20260605.md` |
| 战略复审 | `D:\我的文学\AI对接专用\传话\战略与全案\战略复审报告-20260604.md` |

---

## 五、重要提醒

- Reasonix 改动粗放、缺少逐步确认。Cursor 接手后请逐项检查上述改动是否引入问题，特别是 CSS 色号变化是否影响整体视觉
- reader 浮层是核心交互，确认为"沉浸阅读面板"而非"弹窗"
- 所有采访内容必须遵守合规约束（无真实姓名、无公司名、无具体 SPU、受访者过目）
